

      - name: Move /${ParentOrganization}/${Organization} to /${Organization}
        run: |
          mv /${ParentOrganization}/${Organization}/${Organization} /${Organization}
          ls /${Organization} -lah