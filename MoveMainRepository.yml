

      - name: Move /${ParentOrganization}/${Organization} to /${Organization}
        run: |
          cp -a /${ParentOrganization}/${Organization}/. /${Organization}
          ls /${Organization} -lah