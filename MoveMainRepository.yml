

      - name: Move /${ParentOrganization}/${Organization} to /${Organization}
        run: |
          sudo cp -a /${ParentOrganization}/${Organization}/. /${Organization}
          ls /${Organization} -lah