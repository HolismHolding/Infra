name: Build & Push holism/react-dev:latest

on:
  workflow_dispatch:

jobs:
  buildAndPush:
    runs-on: ubuntu-latest
    steps:
      - name: Get /HolismHolding/Infra
        run: |
          sudo mkdir /HolismHolding
          sudo chmod 777 /HolismHolding
          cd /HolismHolding
          git clone https://github.com/HolismHolding/Infra

      - name: Build holism/react-dev
        run: |
          cd /HolismHolding/Infra/React/Panel/Dev
          docker build -t holism/react-dev:latest .
      - name: Push holism/react-dev
        run: |
          echo ${{ secrets.DOCKER_ACCESS_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker push holism/react-dev:latest
