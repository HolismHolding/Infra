name: Build & Push holism/sql-dev:latest

on:
  workflow_dispatch:

jobs:
  buildAndPush:
    runs-on: ubuntu-latest
    steps:
      - name: Get /HolismHolding/Infra
        run: |
          sudo mkdir /HolismHolding
          sudo chmod 777 /HolismHolding
          cd /HolismHolding
          git clone https://github.com/HolismHolding/Infra

      - name: Build holism/sql-dev
        run: |
          cd /HolismHolding/Infra/SqlServer/Dev
          docker build -t holism/sql-dev:latest .
      - name: Push holism/sql-dev
        run: |
          echo ${{ secrets.DOCKER_ACCESS_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker push holism/sql-dev:latest
